# HHC 项目技术总结

**生成日期**: 2026-02-28  
**工作目录**: `/home/ltan/HHC`

---

## 1. 项目概述

多目标 HHCRSP（Home Health Care Routing and Scheduling Problem）：
- **f₁**：总行驶距离
- **f₂**：总患者等待时间
- **DRL**：WE-Add 多目标强化学习（λ-conditioned policy）
- **MOEA/D**：Tchebycheff 分解多目标进化算法

---

## 2. 数据生成

### 2.1 训练数据（实时生成）

**来源**：`problems/agh/problem_agh.py` 中 `AGHDataset`（`filename=None` 时）

- 每个 epoch 重新随机生成
- 使用 `arrival_prob.npy`、need 概率 (0.116×6, 0.1×3)
- **未使用** `generate_data.py` 或 `generate_test_data.py`

### 2.2 测试数据（DRL vs MOEA/D 对比）

**来源**：`generate_test_data.py`

```bash
python generate_test_data.py --n_instances 1000 --seed 1234 --output_dir paretofront
```

- 输出：`paretofront/shared_test_50.pkl`、`shared_test_100.pkl`
- 固定 seed=1234，保证 DRL 与 MOEA/D 使用同一批实例
- **generate_data.py** 及其输出 `data/agh/` 目前**未被使用**

---

## 3. 模型与 Cost 计算

### 3.1 输入特征（AGHDataset）

| Feature | 含义 | 形状 |
|---------|------|------|
| loc | 登机口索引 (1–100) | [batch, graph_size] |
| arrival | 到达时间（分钟） | [batch, graph_size] |
| departure | 离开时间 | [batch, graph_size] |
| type | 节点类型 (0/1/2) | [batch, graph_size] |
| need | 服务需求 (1–9) | [batch, graph_size] |

### 3.2 Cost 公式

```
cost = λ₁·f₁ + λ₂·f₂
```

- 训练：λ 每样本随机 `λ₁ ~ Uniform(0,1)`，`λ₂ = 1 - λ₁`
- 验证：λ 固定 `(0.5, 0.5)`
- 无 ObjectiveNormalizer（已移除）

### 3.3 Encoder 不接收距离矩阵

`distance` 仅用于 `state_agh` 约束检查与 `get_costs` 中 f₁ 计算，Encoder 通过 `loc_embedding` 间接利用距离信息。

---

## 4. 脚本与流程

### 4.1 训练

| 脚本 | n | epochs | 说明 |
|------|---|--------|------|
| `scripts/train_no_norm_50.sh` | 50 | 100 | raw cost |
| `scripts/train_no_norm_100.sh` | 100 | 100 | raw cost |
| `scripts/train_raw_50_200ep.sh` | 50 | 200 | 收敛验证 |
| `scripts/train_raw_100_200ep.sh` | 100 | 200 | 收敛验证 |

```bash
sbatch scripts/train_no_norm_50.sh
```

### 4.2 DRL Pareto 推理

| 脚本 | 输出 |
|------|------|
| `scripts/comparison_drl_50.sh` | `paretofront/pareto_results_50_raw_101w.pkl` |
| `scripts/comparison_drl_100.sh` | `paretofront/pareto_results_100_raw_101w.pkl` |

自动选择最新 `train_raw_*` checkpoint，使用 `shared_test_*.pkl`。

### 4.3 MOEA/D

| 脚本 | 输出 |
|------|------|
| `scripts/comparison_moead_50.sh` | `paretofront/moead_results_50_exp1.pkl` |
| `scripts/comparison_moead_100.sh` | `paretofront/moead_results_100_exp1.pkl` |

参数：101 权重、500 代、seed 1234。

### 4.4 pymoo MOEA/D（可选）

| 脚本 | 输出 |
|------|------|
| `scripts/comparison_pymoo_50.sh` | `paretofront/pymoo_moead_results_50_exp1.pkl` |
| `scripts/comparison_pymoo_100.sh` | `paretofront/pymoo_moead_results_100_exp1.pkl` |

与 `moead.py` 相同条件，输出 moead 兼容格式。

### 4.5 收敛图

```bash
sbatch scripts/plot_convergence.sh
```

- 使用 `shared_test_50.pkl` 与 `pareto_inference` 一致
- 输出：`images/convergence/convergence.png`
- 缓存：`paretofront/convergence_data.pkl`

仅绘图（不重新评估）：
```bash
python visualization/plot_convergence.py --plot-only --exp_name convergence
```

### 4.6 DRL vs MOEA/D 对比图

```bash
python visualization/plot_moead_comparison.py \
    --graph_size 50 \
    --drl_path paretofront/pareto_results_50_raw_101w.pkl \
    --moead_path paretofront/moead_results_50_exp1.pkl \
    --exp_name raw_101w
```

---

## 5. 关键路径

| 资源 | 路径 |
|------|------|
| 训练 checkpoint | `outputs/agh_{50,100}/<run_name>/epoch-*.pt` |
| 测试数据 | `paretofront/shared_test_{50,100}.pkl` |
| DRL Pareto 结果 | `paretofront/pareto_results_{50,100}_raw_101w.pkl` |
| MOEA/D 结果 | `paretofront/moead_results_{50,100}_exp1.pkl` |
| 收敛图 | `images/convergence/convergence.png` |
| 对比图 | `images/<exp_name>/moead_vs_drl_{50,100}.png` |
| 距离矩阵 | `problems/agh/distance.pkl` |
| 车队信息 | `problems/agh/fleet_info.pkl` |

---

## 6. plot_convergence 与 pareto_inference 一致性

- **数据集**：均使用 `shared_test_50.pkl`（已统一）
- **Cost 公式**：`cost = λ₁·f₁ + λ₂·f₂`
- **λ=(0,1)**：cost = f₂（等待时间），收敛图纵轴 ≈ 0–100
- **λ=(1,0)**：cost = f₁（距离），收敛图纵轴 ≈ 3000

---

## 7. moead.py 约束（与 DRL state_agh 一致）

```python
COMBO_NEED = {3: 7, 5: 8, 6: 9}
LATE_TOLERANCE = {3: 30.0, 5: 30.0, 6: 0.0}
```

- C1：基本时间窗 `svc_start + dur <= tw_right`
- C2：Combo 患者晚到约束（Fleet 3/5/6）

---

## 8. 已知问题与 TODO

1. **DRL 收敛**：100 epoch 可能不足，已启动 200 epoch 实验
2. **Encoder 无距离矩阵**：仅通过 loc embedding 间接利用距离
3. **generate_data.py**：未使用，可视为遗留脚本

---

## 9. 参考文档

- `experiment_summary.md`：实验设计与结果
- `weekly_summary_20260223.md`：工作记录
- `moead_algorithm_doc.md`：MOEA/D 算法说明
